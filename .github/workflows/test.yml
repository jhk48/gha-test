name: Test-workflow

on:
  pull_request:
    branches: [main]
    types: [closed]

jobs:
  frontend:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
